# Agent-scoped memory — <AgentName>

Optional. Notes this agent has accumulated about its work — patterns it's noticed, decisions it's made, references it should remember.

This is **not** the global memory system (which lives in `~/.claude/projects/*/memory/`). This is private, agent-scoped state.

## Decisions made

- (e.g., "Decided to favor metric X over metric Y for sourcing because [reason] — 2025-04-15")

## Patterns noticed

- (e.g., "Founders who lead with metrics rather than narrative tend to score higher in DD")

## References

- (e.g., "Standard valuation frameworks: [link to Brain note]")
