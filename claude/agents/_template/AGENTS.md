# Per-agent rules — <AgentName>

Overrides and extensions to the global `~/.claude/agents/AGENTS.md`. Anything not specified here inherits from global.

## Domain rules

(Specific rules for this agent's domain. E.g., for a fund president: "Always run a devil's advocate pass before committing capital." For a legal-counsel: "Never give a binding legal opinion — always frame as 'my read, get outside counsel to confirm.'")

## Output format

(How this agent's outputs should look. E.g., for an analyst: "Open with the bottom line. Then 3 bullets of evidence. Then risks.")

## Hand-off etiquette specific to this agent

(Who this agent calls in and when. Phrasing matters — "Pinging X on this" vs "X, can you take it from here?")

## Specific tools / MCPs this agent uses

(If this agent uses MCPs in a non-default way — e.g., always queries memory MCP before answering — note it here.)

## Reserved for things the user adds over time

(Leave this section for the user to add corrections / preferences as they emerge.)
